<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.losgai.sys.mapper.CommentIndexMapper">

    <resultMap id="BaseResultMap" type="com.losgai.sys.entity.carRental.CommentIndex">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="carId" column="car_id" />
            <result property="parentCommentId" column="parent_comment_id" />
            <result property="followCommentId" column="follow_comment_id" />
            <result property="hotScore" column="hot_score" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="deleted" column="deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,car_id,parent_comment_id,follow_comment_id,hot_score,create_time,
        update_time,deleted
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from comment_index
        where  id = #{id} 
    </select>

    <select id="queryByCarIdWithLimit" resultType="com.losgai.sys.dto.CommentIndexDto">
        select
            ci.id as id,
            ci.user_id as userId,
            u.username as username,
            u.avatar_url as avatar,
            ci.car_id as carId,
            ci.parent_comment_id as parentCommentId,
            ci.follow_comment_id as followCommentId,
            ci.hot_score as hotScore,
            (select count(1) from comment_index sub where sub.parent_comment_id = ci.id and sub.deleted = 0) as followCount,
            ci.create_time as createTime,
            ci.update_time as updateTime,
            ci.deleted as deleted
        from comment_index ci
        left join user u on u.id = ci.user_id
        where ci.car_id = #{carId}
        and parent_comment_id = 0
        and ci.deleted = 0
        order by ci.hot_score desc,ci.create_time desc
        limit #{limit}
    </select>

    <select id="queryByCarId" resultType="com.losgai.sys.dto.CommentIndexDto">
        select
            ci.id as id,
            ci.user_id as userId,
            u.username as username,
            u.avatar_url as avatar,
            ci.car_id as carId,
            ci.parent_comment_id as parentCommentId,
            ci.follow_comment_id as followCommentId,
            ci.hot_score as hotScore,
            (select count(1) from comment_index sub where sub.parent_comment_id = ci.id and sub.deleted = 0) as followCount,
            ci.create_time as createTime,
            ci.update_time as updateTime,
            ci.deleted as deleted
        from comment_index ci
                 left join user u on u.id = ci.user_id
        where ci.car_id = #{carId}
          and parent_comment_id = 0
          and ci.deleted = 0
        order by ci.hot_score desc,ci.create_time desc
    </select>

    <select id="queryReplyWithLimit" resultType="com.losgai.sys.dto.CommentIndexDto">
        select
            ci.id as id,
            ci.user_id as userId,
            u.username as username,
            u.avatar_url as avatar,
            ci.car_id as carId,
            ci.parent_comment_id as parentCommentId,
            ci.follow_comment_id as followCommentId,
            ci.hot_score as hotScore,
            ci.create_time as createTime,
            ci.update_time as updateTime,
            ci.deleted as deleted
        from comment_index ci
        left join user u on u.id = ci.user_id
        where ci.parent_comment_id = #{parentCommentId}
        and ci.deleted = 0
        order by ci.hot_score desc,ci.create_time desc
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from comment_index
        where  id = #{id} 
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.losgai.sys.entity.carRental.CommentIndex" useGeneratedKeys="true">
        insert into comment_index
        ( id,user_id,car_id,parent_comment_id,follow_comment_id,hot_score,create_time,
        update_time,deleted)
        values (#{id},#{userId},#{carId},#{parentCommentId},#{followCommentId},#{hotScore},#{createTime},
        #{updateTime},#{deleted})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.losgai.sys.entity.carRental.CommentIndex" useGeneratedKeys="true">
        insert into comment_index
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="userId != null">user_id,</if>
                <if test="carId != null">car_id,</if>
                <if test="parentCommentId != null">parent_comment_id,</if>
                <if test="followCommentId != null">follow_comment_id,</if>
                <if test="hotScore != null">hot_score,</if>
                <if test="createTime != null">create_time,</if>
                <if test="updateTime != null">update_time,</if>
                <if test="deleted != null">deleted,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="userId != null">#{userId},</if>
                <if test="carId != null">#{carId},</if>
                <if test="parentCommentId != null">#{parentCommentId},</if>
                <if test="followCommentId != null">#{followCommentId},</if>
                <if test="hotScore != null">#{hotScore},</if>
                <if test="createTime != null">#{createTime},</if>
                <if test="updateTime != null">#{updateTime},</if>
                <if test="deleted != null">#{deleted},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.losgai.sys.entity.carRental.CommentIndex">
        update comment_index
        <set>
                <if test="userId != null">
                    user_id = #{userId},
                </if>
                <if test="carId != null">
                    car_id = #{carId},
                </if>
                <if test="parentCommentId != null">
                    parent_comment_id = #{parentCommentId},
                </if>
                <if test="followCommentId != null">
                    follow_comment_id = #{followCommentId},
                </if>
                <if test="hotScore != null">
                    hot_score = #{hotScore},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime},
                </if>
                <if test="updateTime != null">
                    update_time = #{updateTime},
                </if>
                <if test="deleted != null">
                    deleted = #{deleted},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.losgai.sys.entity.carRental.CommentIndex">
        update comment_index
        set 
            user_id =  #{userId},
            car_id =  #{carId},
            parent_comment_id =  #{parentCommentId},
            follow_comment_id =  #{followCommentId},
            hot_score =  #{hotScore},
            create_time =  #{createTime},
            update_time =  #{updateTime},
            deleted =  #{deleted}
        where   id = #{id} 
    </update>

    <update id="batchUpdateLikeCountMap">
        UPDATE comment_index
        SET like_count = CASE id
        <foreach collection="list" item="item" index="index">
            WHEN #{item.id} THEN #{item.likeCount}
        </foreach>
        END
        WHERE id IN
        <foreach collection="list" item="item" index="index" separator="," open="(" close=")">
            #{item.id}
        </foreach>
    </update>

    <update id="batchUpdateHeat">
        UPDATE comment_index
        SET hot_score =
        CASE id
        <foreach collection="heatDtoList" item="item">
            WHEN #{item.commentId} THEN #{item.delta}
        </foreach>
        ELSE hot_score
        END
        WHERE id IN
        <foreach collection="heatDtoList" item="item" open="(" separator="," close=")">
            #{item.commentId}
        </foreach>
    </update>

</mapper>
